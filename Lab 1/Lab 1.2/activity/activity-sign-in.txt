@startuml
|User|
start

:User enters login credentials;
|System|
:Validate user's credentials;
|Database|
:Retrieve user information from the database;
|System|
if (User credentials are valid) then (Valid)
  :Grant access to the system;
else (Invalid)
  :Show an error message to the user;
endif

|User|
stop
@enduml