@startuml
|User|
start

:User provides registration information;
|System|
:Validate user's registration information;
|Database|
if (User information is unique) then (Unique)
  :Create a new user account;
  :Grant access to the system;
else (Not Unique)
  :Show an error message to the user;
endif

|User|
stop
@enduml